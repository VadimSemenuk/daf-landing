---
const { title, Icon } = Astro.props;
---

<div class="flex flex-col justify-between
    gap-6 md:p-8 p-4 border-1 border-[#ccc] rounded-3xl
    cursor-pointer transition-all duration-200 hover:scale-102">
    <div class="flex">
        <div class="p-4 rounded-2xl bg-primary-light text-primary-dark">
            <Icon class="w-10 h-10" />
        </div>
    </div>
    <h3 class="text-xl">{title}</h3>
</div>